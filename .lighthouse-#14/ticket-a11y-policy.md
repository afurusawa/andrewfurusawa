Part of #15

## Question

Given the research on accessible kitsch patterns, what **accessibility and motion policy** should the `/90s` spec require?

Decide at least:

1. Baseline target (e.g. WCAG 2.2 AA for substance; what latitude for decorative chrome).
2. Motion rules (what may animate by default; what respects `prefers-reduced-motion`; hard bans).
3. Rules for non-functional theater controls (hit counter, badges, "guestbook" facades) so they do not pretend to be working features.
4. Any intentional accessibility trade-offs the author accepts for faithful kitsch — and which trade-offs are forbidden.

Blocked until the accessible-kitsch research ticket is resolved.
