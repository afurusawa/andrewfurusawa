Part of #15

## Question

Given the locked chrome inventory for `/90s` v1, what **asset strategy** should the spec require for must-have and optional chrome?

Decide at least:

1. **Must-have shell**: which pieces are pure CSS (bevels, dividers, counter digits, neon type) vs image/SVG assets.
2. **Optional pack**: under-construction graphics, small badge strip, single background texture — CSS-only, hand-made rasters, or either.
3. **Who produces** non-CSS assets (author, agent-generated, stock/public domain) if any are required.
4. **Format constraints** if images are allowed (e.g. SVG preferred, max weight, no animated GIF autoplay).

Context: cosmetic theater only; lean shell + optional pack; faithful kitsch; secret WIP.
