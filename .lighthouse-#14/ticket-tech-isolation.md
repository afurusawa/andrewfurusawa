Part of #15

## Question

Given the Next.js isolation research, what **technical isolation defaults** should the `/90s` spec lock for implementers?

Decide at least:

1. App Router layout/route structure to use in this repo.
2. How styles and fonts are scoped (and what must not change on `/`).
3. Indexing/SEO defaults (`noindex` or not, sitemap exclusion, robots).
4. Any hard constraints on client JS, third-party embeds, or shared components.

Blocked until the Next.js isolation research ticket is resolved.
